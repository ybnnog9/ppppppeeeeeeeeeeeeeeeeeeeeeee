<section class="section" data-animate>
  <div class="container">
    <div class="section-title">
      <div>
        <span class="badge">{{ section.settings.badge }}</span>
        <h2>{{ section.settings.heading }}</h2>
      </div>
    </div>
    <div class="stats-grid">
      {% for block in section.blocks %}
        <div class="stat-card" {{ block.shopify_attributes }}>
          <h3>{{ block.settings.title }}</h3>
          <p>{{ block.settings.text }}</p>
        </div>
      {% endfor %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Envíos",
  "settings": [
    {"type": "text", "id": "badge", "label": "Etiqueta", "default": "Envío"},
    {"type": "text", "id": "heading", "label": "Título", "default": "Entrega rápida y segura"}
  ],
  "blocks": [
    {
      "type": "item",
      "name": "Detalle",
      "settings": [
        {"type": "text", "id": "title", "label": "Título", "default": "24-72h"},
        {"type": "textarea", "id": "text", "label": "Texto", "default": "Entrega express con tracking en tiempo real."}
      ]
    }
  ],
  "presets": [{"name": "Envíos"}]
}
{% endschema %}
