<section class="section" data-animate>
  <div class="container">
    <div class="section-title">
      <div>
        <span class="badge">{{ section.settings.badge }}</span>
        <h2>{{ section.settings.heading }}</h2>
      </div>
    </div>
    <div class="accessories-grid">
      {% for block in section.blocks %}
        <div class="accessory-card" {{ block.shopify_attributes }}>
          <h3>{{ block.settings.title }}</h3>
          <p>{{ block.settings.text }}</p>
        </div>
      {% endfor %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Accesorios",
  "settings": [
    {"type": "text", "id": "badge", "label": "Etiqueta", "default": "Accesorios"},
    {"type": "text", "id": "heading", "label": "Título", "default": "Complementos recomendados"}
  ],
  "blocks": [
    {
      "type": "item",
      "name": "Accesorio",
      "settings": [
        {"type": "text", "id": "title", "label": "Título", "default": "Kit de instalación"},
        {"type": "textarea", "id": "text", "label": "Texto", "default": "Complemento ideal para una instalación rápida."}
      ]
    }
  ],
  "presets": [{"name": "Accesorios"}]
}
{% endschema %}
